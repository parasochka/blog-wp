# Dependencies
node_modules/
vendor/

# Build artifacts
*.zip
dist/
build/

# Design source binary — the live tokens ship as CSS in _ds/…/tokens/*.css,
# so the Figma export need not be committed.
*.fig

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log

# Local WordPress / secrets
wp-config.php
.env
.env.*
